# 下一步

1. 替换所有占位内容为最终版本（文本、时间线、成就细节）。
2. 增加项目截图与真实链接。
3. 用正式 `Sam_Gu_Resume.pdf` 替换 `assets/resume/sam-gu-resume.html`。
4. 如有需要：加入文章分页、轻量脚本（年份自动渲染）和 SEO metadata。

## 待确认信息
- 每个项目的真实贡献说明（尤其：`zhenyan-monitoring`、`cds527`）
- 是否启用单独的头像图像与色彩微调
- 简历最终文件格式与路径

- 下一次建议：核对所有 `href` 是否在子目录下正常跳转。
