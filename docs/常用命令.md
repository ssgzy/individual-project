# 常用命令

- `python3 -m http.server 8080`：本地预览
- `ls -la`：检查当前目录结构
- `open index.html`：macOS 下快速打开首页

- 更新与维护：
  - 修改页面文本：直接编辑对应 `*.html`
  - 统一样式：`styles/main.css`
  - 文档更新：`docs/` 下对应 markdown

- 部署前检查清单：
  - `.nojekyll` 是否存在
  - 所有页面是否有返回顶部导航
  - 内链是否走相对路径
